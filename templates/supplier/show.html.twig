{% extends 'base.html.twig' %}

{% block body %}
  <h1>supplier</h1>

  <table>
    <tbody>
    <tr>
      <th>Id</th>
      <td>{{ supplier.id }}</td>
    </tr>
    <tr>
      <th>Name</th>
      <td>{{ supplier.name }}</td>
    </tr>
    </tbody>
  </table>

  <ul>
    <li>
      <a href="{{ path('supplier_index') }}">Back to the list</a>
    </li>
  </ul>
{% endblock %}